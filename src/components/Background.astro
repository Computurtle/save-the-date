---

---

<div class="background-container">
    <div class="gradient-mesh"></div>
    <div class="noise-overlay"></div>
</div>

<style>
    .background-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        overflow: hidden;
        background-color: var(--color-bg);
    }

    /*
    Creates a soft, watercolor-like wash of the theme colors.
    Using radial gradients instead of moving divs prevents the "dizzy" blur effect.
  */
    .gradient-mesh {
        position: absolute;
        inset: -50%; /* Extend well beyond viewport to allow movement */
        width: 200%;
        height: 200%;
        background-image:
            radial-gradient(
                circle at 50% 50%,
                rgba(255, 255, 255, 0.1) 0%,
                transparent 60%
            ),
            radial-gradient(
                circle at 0% 0%,
                rgba(155, 135, 213, 0.5) 0%,
                transparent 60%
            ),
            /* Lavender */
                radial-gradient(
                    circle at 100% 100%,
                    rgba(139, 168, 136, 0.6) 0%,
                    transparent 60%
                ),
            /* Sage */
                radial-gradient(
                    circle at 100% 0%,
                    rgba(155, 135, 213, 0.45) 0%,
                    transparent 60%
                ),
            /* Lavender */
                radial-gradient(
                    circle at 0% 100%,
                    rgba(139, 168, 136, 0.5) 0%,
                    transparent 60%
                ); /* Sage */
        filter: blur(80px);
        animation: subtle-shift 30s ease-in-out infinite alternate;
        opacity: 1;
    }

    /*
    Adds a static film grain texture.
    This grounds the floaty gradients and gives it a high-quality paper/cardstock feel.
  */
    .noise-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0.07;
        pointer-events: none;
        z-index: 1;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    }

    @keyframes subtle-shift {
        0% {
            transform: translate(0, 0) scale(1);
        }
        100% {
            transform: translate(-2%, -2%) scale(1.05);
        }
    }
</style>
